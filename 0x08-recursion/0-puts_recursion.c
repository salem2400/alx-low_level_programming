#include "main.h"

/**
 * main - check the code
 * Return: Always 0.
 */
int main(void)
{
	_puts_recursion("Puts with recursion");
	if (s == '\0')
	{
		_putchar('\n');
		return (0);
	}
	_putchar(*s);
	_putchar(s + 1);
}
